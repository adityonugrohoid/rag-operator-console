# Base image for all RAG Operator Console services
# Contains common dependencies without heavy ML libraries
FROM python:3.12-slim

WORKDIR /app

# Install base dependencies (no sentence-transformers)
COPY requirements-base.txt .
RUN pip install --no-cache-dir -r requirements-base.txt

# This base image is ~500MB instead of ~13GB
